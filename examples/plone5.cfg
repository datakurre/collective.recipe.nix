[buildout]
extends = http://dist.plone.org/release/5.0b3/versions.cfg
parts = plone
versions = versions

[instance]
recipe = plone.recipe.zope2instance
eggs = Plone
user = admin:admin
var = /tmp

[plone]
recipe = collective.recipe.nix
parts = instance
eggs = ${instance:eggs}
version = 5.0b3
outputs = plone.nix

[versions]
zc.buildout =
setuptools =
Pillow =
